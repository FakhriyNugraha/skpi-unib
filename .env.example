APP_NAME="SKPI UNIB"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_TIMEZONE=Asia/Jakarta
APP_LOCALE=id
APP_FALLBACK_LOCALE=id
APP_FAKER_LOCALE=id_ID

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=skpi_unib
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_CONNECTION=log
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120
QUEUE_CONNECTION=sync
FILESYSTEM_DISK=local

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="no-reply@skpi-unib.local"
MAIL_FROM_NAME="${APP_NAME}"

# GOOGLE DRIVE SERVICE ACCOUNT CREDENTIALS
# For local development, use file approach:
# GOOGLE_DRIVE_SERVICE_ACCOUNT_FILE=skpiunib-credential.json
# For production (Laravel Cloud), use base64 approach:
GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON_BASE64= # Paste your base64 encoded credential here

# IMPORTANT: Only set ONE of the above variables, not both
# For Laravel Cloud, ALWAYS use the base64 approach

VITE_APP_NAME="${APP_NAME}"
